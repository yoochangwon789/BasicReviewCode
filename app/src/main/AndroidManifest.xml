<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yoochangwonspro.basicreviewcode">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicReviewCode">
        <activity android:name=".TomatoTimerActivity"
            android:theme="@style/Theme.BasicReviewCodeNoTopBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailElectronicPictureFrameActivity" />
        <activity android:name=".ElectronicPictureFrameActivity">

        </activity>
        <activity android:name=".CalculatorActivity" />
        <activity
            android:name=".DetailDiaryActivity"
            android:theme="@style/Theme.BasicReviewCodeNoTopBar" />
        <activity
            android:name=".SecretDiaryActivity"
            android:theme="@style/Theme.BasicReviewCodeNoTopBar" />
        <activity android:name=".LottoLotteryActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>